// var examples = $("svg").svgPanZoom({
//     initialViewBox: {
//         x: 0,
//         y: 0,
//         width: 500,
//         height: 500
//     }
// });

SVGEditor.prototype.initIndex = function () {
    var svg = this.svg;

    /*SVG.get('line').selectize({
     deepSelect: true
     }).resize();

     SVG.get('polyline').selectize({
     deepSelect: true
     }).resize();*/

    //patten
    //path.setAttribute("fill", "notes");
};